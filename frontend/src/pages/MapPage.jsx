import React,{useEffect,useState}from'react';import{MapContainer,TileLayer,Marker,Popup,useMap}from'react-leaflet';import L from'leaflet';import{getMap}from'../services/api';import PriorityBadge from'../components/PriorityBadge';import{Link}from'react-router-dom';
const icon=(level)=>L.divIcon({className:'',html:`<div style="width:18px;height:18px;border-radius:50%;background:${level==='High'?'#fb7185':level==='Medium'?'#fbbf24':level==='Low'?'#34d399':'#a78bfa'};border:3px solid #062532;box-shadow:0 0 0 3px rgba(255,255,255,.12)"></div>`,iconSize:[18,18],iconAnchor:[9,9]});
export default function MapPage(){const[data,setData]=useState([]),[filter,setFilter]=useState('All');useEffect(()=>{getMap().then(setData)},[]);const shown=data.filter(x=>filter==='All'||x.priority===filter);return <div className="h-screen flex flex-col"><div className="px-5 md:px-8 py-4 border-b border-white/5 flex flex-wrap justify-between items-center gap-3"><div><div className="font-black text-xl">MarineGuard GIS</div><div className="text-xs soft">OpenStreetMap · community observation priority</div></div><div className="flex gap-2 flex-wrap">{['All','High','Medium','Low','Needs Review'].map(x=><button key={x} onClick={()=>setFilter(x)} className={`px-3 py-2 rounded-lg text-xs font-bold ${filter===x?'bg-ocean-500 text-ocean-950':'bg-white/5 soft'}`}>{x}</button>)}</div></div><div className="flex-1 p-3"><MapContainer center={[21.59,39.15]} zoom={10} scrollWheelZoom className="!rounded-2xl"><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{shown.map(x=><Marker key={x.id} position={[x.latitude,x.longitude]} icon={icon(x.priority)}><Popup><div style={{minWidth:220}}><b>{x.site_name}</b><div style={{marginTop:6}}>{x.description}</div><div style={{marginTop:8}}><b>Priority:</b> {x.priority} · {x.score}/100</div><div style={{marginTop:10}}><a href={`/reports/${x.id}`}>Open case →</a></div></div></Popup></Marker>)}</MapContainer></div></div>}
