import React,{useEffect,useState}from'react';import{useParams,Link}from'react-router-dom';import{getSite}from'../services/api';import{ArrowLeft,MapPin}from'lucide-react';export default function Site(){const{id}=useParams();const[s,setS]=useState(null);useEffect(()=>{getSite(id).then(setS)},[id]);if(!s)return <div className="p-10 soft">Loading site…</div>;return <div className="p-5 md:p-8 max-w-6xl mx-auto"><Link to="/map" className="text-sm soft flex items-center gap-2 mb-5"><ArrowLeft size={15}/>Back to map</Link><div className="panel p-6"><div className="flex items-start gap-4"><div className="h-12 w-12 rounded-2xl bg-ocean-500/10 text-ocean-500 flex items-center justify-center"><MapPin/></div><div><h1 className="text-2xl font-black">{s.site.name}</h1><div className="text-sm soft mt-1">{s.site.latitude.toFixed(4)}, {s.site.longitude.toFixed(4)}</div></div></div><div className="mt-8"><h2 className="font-bold">Observation timeline</h2><div className="mt-4 space-y-3">{[...s.history,...s.reports.map(r=>({type:'community_report',created_at:r.created_at,evidence:{description:r.description}}))].sort((a,b)=>new Date(a.created_at)-new Date(b.created_at)).map((x,i)=><div key={i} className="flex gap-4"><div className="w-24 text-xs soft pt-1">{new Date(x.created_at).toLocaleDateString()}</div><div className="flex-1 p-4 rounded-xl bg-white/[.03] border border-white/5"><div className="font-semibold capitalize">{x.type.replaceAll('_',' ')}</div><div className="text-xs soft mt-1">{x.source||'MarineGuard'}</div><div className="text-sm soft mt-2">{x.evidence?.description||x.evidence?.note||'Observation stored in site history.'}</div></div></div>)}</div></div></div></div>}
